<h1>Listing Flags</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Is on</th>

      <th></th>
    </tr>
  </thead>
  <tbody>
<%= for flag <- @flags do %>
    <tr>
      <td><%= flag.name %></td>
      <td><%= flag.is_on %></td>

      <td>
        <%= link "Show", to: Routes.dashboard_organization_project_flag_path(@conn, :show, @organization, @project, flag) %>
        <%= link "Edit", to: Routes.dashboard_organization_project_flag_path(@conn, :edit, @organization, @project, flag) %>
        <%= link "Delete", to: Routes.dashboard_organization_project_flag_path(@conn, :delete, @organization, @project, flag), method: :delete, data: [confirm: "Are you sure?"] %>
      </td>
    </tr>
<% end %>
  </tbody>
</table>

<span><%= link "New Flag", to: Routes.dashboard_organization_project_flag_path(@conn, :new, @organization, @project ) %></span>